// config.js

CKFinder.define( function( config ) {
    config.language = 'en';
    config.uiColor = '#ffffff';
    config.filebrowserUploadUrl = '/path/to/upload_endpoint'; // Đường dẫn đến điểm cuối xử lý tải lên

    // Thêm cấu hình khác nếu cần
} );
